# home-ops stack — DO NOT FILL IN BY HAND.
# Run `scripts/generate-env.sh` to bootstrap ./.env with strong secrets.
# Keys listed here so editors / Compose can introspect what's expected.
POSTGRES_PASSWORD=
INGEST_TOKEN=
LOGS_PASSWORD=
