# GPU scheduler — list of game executable names (lowercase) to treat as
# "gaming" without waiting for the GPU threshold.
# One per line. # comments allowed.
# Add the actual .exe names you launch — easiest way to find them is to run
# the game once and the scheduler logs `foreground_exe=<name>` events you
# can grep in the viewer.
steam.exe
csgo.exe
cs2.exe
gta5.exe
eldenring.exe

# Battle.net / Diablo IV — names vary by install language; psutil returns
# the process name lowercased, with spaces preserved.
diablo iv.exe
diablo4.exe
diablo.exe
battle.net.exe
battle.net launcher.exe
agent.exe
