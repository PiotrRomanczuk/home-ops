# GPU scheduler — list of game executable names (lowercase) to treat as
# "gaming" without waiting for the GPU threshold.
# One per line. # comments allowed.
# Add the actual .exe names you launch — easiest way to find them is to run
# the game once and the scheduler logs `foreground_exe=<name>` events you
# can grep in the viewer.
#
# Do NOT add tray-resident launchers (steam.exe, battle.net.exe,
# agent.exe) — those run all the time without using the GPU, and
# putting them here makes the scheduler refuse all work indefinitely.
# Only list the actual game .exe that loads the GPU.

csgo.exe
cs2.exe
gta5.exe
eldenring.exe
diablo iv.exe
diablo4.exe
diablo.exe
